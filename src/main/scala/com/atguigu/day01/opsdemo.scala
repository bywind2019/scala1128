package com.atguigu.day01

import java.io.BufferedWriter

import scala.io.StdIn

object opsdemo {
    def main(args: Array[String]): Unit = {
//        val a = 10
//        val b = 20
//        val max = if(a > b) a else b
//        val max = a.min(b)
//        println(max)
        
       /* var b = 20
        var a = b = 30
        println(a)
        println(b)
        
        println(b = 100)
        
        val c = {10;20}
        println(c)*/
        
        val line: String = StdIn.readLine("请输入一个字符串: ")
        println(line)
    }
}

/*
在scala中, 任何的语法结构都有类型和值

赋值语句的值是: Unit  ()

 */